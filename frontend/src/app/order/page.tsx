import { PageShell } from "@/components/PageShell";
import { Typography } from "@mui/material";

export default function OrderPage() {
  return (
    <PageShell title="Order" description="Order review, fulfillment, and status tracking workflows.">
      <Typography color="text.secondary">Order module placeholder.</Typography>
    </PageShell>
  );
}
